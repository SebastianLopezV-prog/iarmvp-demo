"""Streamlit dashboard entrypoint (Task 4.1) — UI only.

Placeholder skeleton (Task 1.1). The dashboard reads exclusively through
``iar.service`` and renders: IaR curve(s) vs limits, a colour-coded MTU/hour
risk heatmap, a Gross/Spread toggle, confidence + portfolio/area selectors,
alert status, and the backtest calibration panel.

Run (once implemented):  ``streamlit run app/dashboard.py``
"""


def main() -> None:
    """Render the dashboard. Implemented in Task 4.1."""
    raise NotImplementedError("Dashboard is implemented in Task 4.1.")


if __name__ == "__main__":
    main()
