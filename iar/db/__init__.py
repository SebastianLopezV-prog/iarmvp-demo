"""Database layer — SQLite is the integration hub for the whole PoC.

Modules
-------
- ``models``  : SQLAlchemy ORM models for all entities (Task 1.2).
- ``session`` : engine creation, ``init_db()``, and the session factory (Task 1.2).
"""
