"""Engine validation tests (Task 2.5) — placeholder.

Will cover: known analytic cases (all-Normal collapses to a closed-form
VaR-like number), P05 convergence vs scenario count, seed reproducibility, and
the explicit "summed-quantile, not summed per-MTU IaR" check.
"""
