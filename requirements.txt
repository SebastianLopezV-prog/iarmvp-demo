# Public dependencies for the hosted DEMO app (app/demo_app.py).
# Streamlit Community Cloud installs from this file. The demo runs the real engine
# and backtest on synthetic data, so it needs NONE of the private/external packages
# (optimeering, optipyclient, windsim) and NO API key. Do not add them here.
numpy
pandas
scipy
SQLAlchemy>=2.0
streamlit
plotly
