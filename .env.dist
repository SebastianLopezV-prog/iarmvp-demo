# Environment variables for the IaR demo. Copy to `.env` and fill in as needed.
# The demo runs fully synthetic, so NO variable is required to run it.

# Optional: when set (to any value), the hosted dashboard advances the synthetic data
# forward on a timer while the app is open (see app/bootstrap.py).
IAR_DEMO_AUTOREFRESH=

